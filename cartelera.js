function checarHorarios(titulo) {
    alert(`Mostrando horarios para: ${titulo}`);
    // Aquí podrías redirigir a una página específica de horarios o mostrar un modal con los horarios disponibles.
    // window.location.href = `horarios.html?titulo=${encodeURIComponent(titulo)}`;
}
